package Enumerativi;

public enum Enum_senza_costruttore {
	Marco, Franco, Giuseppe, Salvatore; //Qui non c'è il costuttore perchè non ci sono altri parametri oltre alla classe classica
	/*Costruttore istanziato automaticamente a vuoto perchè non ci sono altri parametri*/
	//Il costruttore viene invocato automaticamente dalla classa chiamante ogni volta che si crea una variabile di tipo enum (non c'è per nessun motivo il new)
}
